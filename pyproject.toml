[project]
name = "lisaastrophysicalbackgrounds"
description = "This package can be used to create an astrophysical gravitational wave background for a specific star formation history and galaxy catalogue."
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE.txt"]
dynamic = ["version"]
dependencies = [
    "astropy>=7.2.0",
    "h5py>=3.15.1",
    "jax",
    "lisaconstants",
    "matplotlib>=3.10.8",
    "pandas>=2.3.3",
    "pyyaml>=6.0",
    "tqdm>=4.68.4",
]

[project.optional-dependencies]
cuda12x = [
    "jax[cuda12]",
]
cuda13x = [
    "jax[cuda13]",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "numpy>=2.0",
    "pytest>=9.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/lisaastrophysicalbackgrounds/__init__.py"

[tool.hatch.build.targets.sdist]
exclude = [
    "/data/populations",
    "/results",
]

# restrict dependencies if jax is giving issues on GPU, e.g., on specific clusters
# [tool.uv]
# override-dependencies = [
#     "nvidia-cublas-cu12; sys_platform == 'never'",
#     "nvidia-cusolver-cu12; sys_platform == 'never'",
#     "nvidia-cusparse-cu12; sys_platform == 'never'",
#     "nvidia-cufft-cu12; sys_platform == 'never'",
#     "nvidia-nvjitlink-cu12; sys_platform == 'never'",
# ]
# constraint-dependencies = [
#     "nvidia-cuda-runtime-cu12>=12.8,<12.9",
#     "nvidia-cublas-cu12>=12.8,<12.9",
#     "nvidia-cuda-nvrtc-cu12>=12.8,<12.9",
#     "nvidia-cuda-cccl-cu12>=12.8,<12.9",
#     "nvidia-cuda-cupti-cu12>=12.8,<12.9",
#     "nvidia-cuda-nvcc-cu12>=12.8,<12.9",
#     "nvidia-nvjitlink-cu12>=12.9,<12.10",
#     "nvidia-cusolver-cu12>=11.7,<11.8",  
#     "nvidia-cusparse-cu12>=12.5,<12.6",  
#     "nvidia-cufft-cu12>=11.4,<11.5",      
# ]

# python guidelines as defined for LISA
[tool.ruff]
# kept in step with requires-python above
target-version = "py312"
line-length = 88

[tool.ruff.lint]
# Select rules:
# E/W = PEP 8 errors/warnings
# F = Pyflakes
# I = Import sorting (isort replacement)
# D = Docstring style (pydocstyle replacement for PEP 257)
# UP = Pyupgrade (ensures modern f-strings, type hints, etc.)
select = ["E", "W", "F", "I", "D", "UP"]

# Optional: Adjust docstring convention to match PEP 257 (Google or NumPy styles also available)
[tool.ruff.lint.pydocstyle]
convention = "pep257"
